spring:
  mail:
    host: ${SMTP_HOST:smtp-relay.brevo.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USER:}
    password: ${SMTP_PASS:}
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true
  datasource:
    url: jdbc:postgresql://ep-autumn-hill-ao8yoixr-pooler.c-2.ap-southeast-1.aws.neon.tech:5432/neondb?sslmode=require
    username: neondb_owner
    password: npg_uN7xk5JDMtai
    hikari:
      connection-timeout: 15000
      maximum-pool-size: 5
      minimum-idle: 2
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

app:
  email:
    from: nss@viit.ac.in    # must be verified as a sender in Brevo

logging:
  level:
    com.nssplatform: INFO
    org.springframework.security: WARN
    org.hibernate: WARN

server:
  servlet:
    session:
      cookie:
        secure: true

# Secure JWT secret
app:
  jwt:
    secret: ${JWT_SECRET:verysecuresecret}